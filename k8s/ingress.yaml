apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: axion-telemetry-query-service-ingress
  annotations:
    konghq.com/plugins: rate-limiting-plugin, cors-plugin
spec:
  ingressClassName: kong
  rules:
    - host: query.axion.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: axion-telemetry-query-service
                port:
                  number: 8000
